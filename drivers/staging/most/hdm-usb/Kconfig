config HDM_USB
	tristate "HDM_USB"

	---help---
	  Say Y here if you want to communicate via V4L2 devices using
	  the interface of hdm usb.

	  To compile this driver as a module, choose M here: the
	  module will be called hdm-usb
